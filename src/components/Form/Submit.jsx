import React from 'react'

const Submit = ({text}) => {
    return (
        <>
            <button type="submit" className="btn w-100 submit fw-bold text-capitalize">{text}</button>
        </>
    )
}

export default Submit
